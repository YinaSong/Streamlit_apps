# 类目分析报告设计说明（完善版 v3）

> 本版在《完善版》基础上，依据 `修改点与修改方向.md` 的 6 条修改意见，并重新核对 `data/` 真实结构后修订。
> 核心变更：① 可视化从 Plotly 整体切换为 **Streamlit 原生图表 + ECharts**；② 色彩统一简约；③ Hello 页补充数据时间来源、当月/上月、环比/同比；④ 品牌页补充「占比第一品牌」与「AOSU 排名」；⑤ 市场历史趋势页重做；⑥ 参考 `Dome Surveillance Cameras.pdf` 的分析角度（趋势 / 季节性 / 品牌集中度 / ASIN 气泡）。
>
> v4 新增（对照 `Dome Surveillance Cameras.pdf` 逐页核对「可直接实现」清单）：品牌/店铺价格定位、Listing 销量集中度、价格/评分区间、店铺对比、ASIN 装修占比、标题词云/词组、新品属性词挖掘（见 §5.2~5.5）。
>
> v5 新增（关键词页闲置字段入图，丰富报告）：竞价/竞争度散点、转化率/垄断性散点、年度搜索量趋势、复合增长率散点（见 §5.4）。

---

## 1. 项目介绍

### 1.1 项目目标

基于 Sorftime 专业版导出的类目分析数据，开发一个 Streamlit 数据开发系统，实现：

- 类目市场分析（总览）
- 品牌竞争分析
- ASIN 竞争分析
- New Release 分析
- 关键词分析
- 全球市场分析
- 市场历史趋势分析

定位：交互式 BI Dashboard，替代 Excel，供运营与管理层快速查看市场情况。

### 1.2 技术栈

| 层 | 技术 |
|---|---|
| Framework | Python 3.12+、Streamlit（`layout="wide"`，统一主题见 `.streamlit/config.toml`） |
| 可视化 | **Streamlit 原生图表**（`st.line_chart`/`st.area_chart`/`st.bar_chart`/`st.scatter_chart`）+ **ECharts**（`streamlit-echarts`，负责横向条、环形、Treemap、帕累托、双轴、热力图、气泡） |
| 数据处理 | Pandas、Numpy、openpyxl |
| 主题 | `.streamlit/config.toml`（主色 `#4E79A7`，浅色主题，统一原生图表配色） |

> 已移除 Plotly、streamlit-option-menu、streamlit-aggrid、streamlit-extras、Pillow —— 均未实际使用（导航直接用 Streamlit multipage 自动生成）。

### 1.3 提供的数据（已与 data/ 实际文件核对）

| 数据域 | 实际文件 | Sheet |
|---|---|---|
| 销量趋势 | `*_售出件数趋势.xlsx` | 近三个月 / 全部月份 |
| 市场历史 | `*_市场趋势.xlsx`、`*_市场趋势 (1).xlsx` | 类目历史数据 |
| 全球对比 | `*_全球售卖对比分析.xlsx` | Sheet1 |
| 品牌 | `BSR品牌销量.xlsx` | 列表视图 |
| ASIN | `BSR ASIN产品列表.xlsx` | 产品详情 / 产品 / 销量趋势 / 销额趋势 |
| 关键词 | `BSR相关关键词.xlsx` | 产品 |
| 词云 | `*_Sorftime流量圈词云详情表-*.xlsx` | Sorftime流量圈词云 |
| 新品 | `New Realese 产品列表.xlsx` | 产品详情 / 产品 / 子体 / 销量趋势 / 销额趋势 |

> 文件名带日期前缀（如 `20260803_Bullet Cameras_市场趋势.xlsx`），数据加载器用 **glob 模式 + 配置映射** 解析，见 §3.4。

---

## 2. 数据字典（核心）

### 2.1 售出件数趋势（销量趋势）

- Sheet：`近三个月`、`全部月份`（两个 Sheet 均为同一全量历史，仅日期格式不同）。
- 字段：`日期`、`售出件数`、`净销售额($)`。
- 行数：约 33 个月（2023-12 ~ 2026-07）。
- 关键点：
  - `近三个月` 日期带 `()` 后缀（`2023-12()`），`全部月份` 不带。**统一去 `()` 后转日期**。
  - **最后一月（2026-07）为导出时点当月，数值明显偏小（≈7.6万 vs 常态 40~50万），属未完整统计**，环比/同比展示需标注「数据可能不完整」。
  - 用于市场销量/销售额月度趋势、当月/上月 KPI、环比（MoM）/同比（YoY）。

### 2.2 市场趋势（宽表，需转置）

- Sheet：`类目历史数据`，**转置结构**：第 1 行表头，第 2 行元信息，第 3 行为月份轴（`202608`→`201809`，约 95 个月），第 4~8 行为指标行。
- 两个文件是**同一导出、不同指标**，必须合并。合并后共 **10 个指标**（原文档误写 11，已修正）：

| 文件 | 指标行 |
|---|---|
| `*_市场趋势.xlsx` | 类目销量、类目销售额、核心词流量、平均品牌数量、平均卖家数量 |
| `*_市场趋势 (1).xlsx` | 平均售价、平均评价、平均星级、平均单个利润、平均大类排名 |

- 元信息：`NODEID=14241331`、`一级大类=Tools & Home Improvement`、`类目名称=Bullet Cameras`、`Listing月销量=226298`。
- 关键点：
  - `类目销售额` 在 2024-01 之前为 `--`；`核心词流量` 只覆盖最近约 56 个月。
  - `平均大类排名` 在 2025-05~2026-04 出现跳变（千位级异常值），仅如实展示。
  - 最新月 `202608` 为未完整月份（类目销量 ≈1.5万），趋势图需标注。

### 2.3 全球售卖对比分析

- Sheet：`Sheet1`，14 行 = 14 个站点。
- 字段：`站点`、`相似类目`、`销售额Top100预估Listing月销量`、`货币`、`Top100预估月销额`、`前3产品销量垄断系数(%)`、`3个月内新品占比(%)`。
- 站点：美国/英国/德国/法国/印度/加拿大/日本/西班牙/意大利/墨西哥/中东/澳洲/澳巴/沙特。
- 关键点：数值是**带千分位字符串** → 去逗号转数值；澳洲、沙特为「该站点未找到相似类目」→ 标记无数据。

### 2.4 品牌销量

- Sheet：`列表视图`，20 个品牌。
- 字段：`序号`、`品牌名称`、`品牌下产品数`、`品牌产品listing月销量`、`品牌产品listing月销额($)`、`市场份额-产品销量份额占比(%)`、`市场份额-产品销售额份额占比(%)`、`品牌下新品数量`、`品牌下新品销量`、`品牌下新品份额(%)`。
- 关键点：
  - 份额字段为 float（`8.529999...`），需 round。
  - **品牌名存在大小写不一致**（如 `aosu` / `eufy Security` / `TP-Link`），AOSU 匹配需 case-insensitive。
  - 占比第一品牌 = **eufy Security**（销售额份额 29.45%、销量份额 15.6%）；**AOSU（aosu）** 销售额份额 8.53%，按销售额份额排名第 4。

### 2.5 BSR ASIN 产品列表

- `产品详情`（100 个 Top 产品）核心字段（57 列）：
  `序号、主图、产品名称、ASIN、URL、ParentAsin、实际价格($)、预计Listing月销量、Listing月销售额($)、Listing年销量、单个产品毛利($)、单个产品毛利率(%)、评分星级、评价数量、品牌、店铺、上架时间、上架天数、FBA费用($)、所在类目、大类排名、类目路径、平均大类排名、BBX卖家属性、物流方式、划线价($)、近7日是否出现库存紧张、近7日是否出现断货、排名变化、排名变化率、细分类目、细分类目排名、单个产品跟卖数量、单个产品变体数量、主图视频、A+、是否气候承诺友好标、是否做品牌旗舰店、国籍/地区、体积（in³）、重量(lb)、重量(g)、体积重量、尺寸标准、尺寸（in）、尺寸（cm）、是否抛货、产品类型、目前销售价($)、Coupon、优惠($)、近30日是否出现库存紧张、近30日是否出现断货、月度同品牌销量占比(市场份额%)、月度流量圈销量占比(%)、FBM费用($)`。
- `产品`（汇总，KPI 来源）：key-value 布局，首行含 **导出时间**（`导出时间：2026-07-01 16:04:36`）。关键指标：产品数=100、月总销量=302305、月总销售额=$31,766,044.85、品牌数量=57、卖家数量=55、类目下产品数=27542、可售产品数=18019、平均价格=$104.12、平均评价数量=3441、平均星级=4.3、A+占比=85%、新品占比(默认三个月)=8%、新品市场份额=2.72%。
- `销量趋势`/`销额趋势`：宽表，`ASIN、ParentASIN` + 24 个月列，销额早期为 `--`。
- **Top20 产品表需展示 `ASIN` + `URL`（可点击跳转 Amazon）**，URL 字段已存在于 `产品详情`。

### 2.6 相关关键词

- Sheet：`产品`，约 2002 个关键词。
- 字段（26 列）：`关键词、旺季、cpc精准竞价($)、cpc最低竞价($)、cpc最高竞价($)、周搜索排名、月搜索量、年搜索量-2026年、年搜索量-2025年、年搜索量-2024年、近90日搜索转化率（%）、近90日点击转化率（%）、坑位竞争、自然位竞争、广告位竞争、词搜索转换比(%)、90天购买量、竞品数量、周搜索排名变化、词搜索量复合增长率-近3个月(%)、词搜索量复合增长率-近6个月(%)、词搜索量复合增长率-近12个月(%)、曝光点击垄断性(%)、曝光转化垄断性(%)、点击占比TOP3 ASIN、转化占比TOP3 ASIN`。
- 关键点：
  - 列名含富文本与全角/半角括号混用（`近90日搜索转化率（%）` 为全角、`词搜索转换比(%)` 为半角），清洗时用常量列名引用。
  - `坑位竞争 / 自然位竞争 / 广告位竞争` **已是 3 个独立字符串列**（格式 `255 / 81（3.15）`），无需再拆分（原文档 §2.6 误判为需解析，已修正）；v5 起由 `kpi.competition_ratio()` 解析出「竞争度比值」（前两数之商）用于「自然位 vs 广告位」散点。
  - `词搜索转换比(%)` 与 `近90日搜索转化率（%）` **数值完全一致**（冗余列），不再单独出图。
  - `点击占比TOP3 ASIN` / `转化占比TOP3 ASIN` 为多行文本，暂不参与图表。

### 2.7 词云详情表

- Sheet：`Sorftime流量圈词云`，**前 2 行是标题/制表日期**，第 3 行才是表头，100 个关键词。
- 字段：`类型、排名、关键词、翻译、重复次数、重复次数占比（%）、关键词产品月销量、关键词产品月销量占比（%）`。

### 2.8 New Release 产品列表

- `产品详情`（70 个新品）：字段 = BSR `产品详情` 57 列 **+** 附加属性列（五点描述、Set name、Recommended Uses For Product…等）。
- `产品`（汇总）：首行含导出时间（2026-07-01 18:37:29）；产品数=70、月总销量=9773、月总销售额=$754,878.84、新品占比=45.71%、新品市场份额=96.61%、平均价格=$124.10、平均评价=37、平均星级=4.54。
- `子体`（变体，121 行）、`销量趋势`/`销额趋势`：宽表。

---

## 3. 数据规范

### 3.1 读取原则（强化）

- 所有 Excel **只读一次**，用 `@st.cache_data` 缓存**清洗后的 DataFrame**（不缓存 `read_excel`）。
- 页面只调用 `load_all()`，禁止重复 `read_excel()`。
- 缓存键含文件 mtime，数据更新后自动失效。

### 3.2 目录结构

```
类目分析报告/
├── Hello.py                      # 主页（总览），唯一入口页
├── pages/
│   ├── 1_BSR 品牌分析.py
│   ├── 2_BSR ASIN分析.py
│   ├── 3_BSR 关键词分析.py
│   ├── 4_New Release分析.py
│   ├── 5_市场趋势分析.py
│   └── 6_全球市场分析.py
├── utils/
│   ├── data_loader.py            # 数据加载 + 清洗 + 缓存（唯一 read_excel 入口）
│   ├── formatters.py             # 千分位/百分比/货币/日期/环比 统一格式化
│   ├── charts.py                 # 图表工厂（Streamlit 原生 + ECharts option）
│   ├── kpi.py                    # KPI / 环比 / 同比 / 季节性 计算函数
│   └── theme.py                  # 配色/字体/高度 常量
├── components/
│   └── ui.py                     # set_page_config、KPI 卡片、使用说明等可复用 UI
├── .streamlit/
│   └── config.toml               # 全局主题（主色/背景/字体）
├── data/                         # 原始 Excel（只读）
└── requirements.txt
```

### 3.3 清洗规则（`utils/data_loader.py`）

1. **日期**：`2023-12()` → 去 `()` → `pd.to_datetime(...)`；市场趋势 `202608` → `YYYYMM` 字符串。
2. **千分位字符串**：`"302,305"` → `302305`。
3. **缺失占位**：`--`、`该站点未找到相似类目` → `NaN`（保留原始列备查）。
4. **市场趋势转置**：宽表 melt 成长表（月份 × 指标），合并两文件得 10 指标；**剔除尾部空月份行**（月份行之后的空单元格）。
5. **销量/销额趋势转置**：ASIN 宽表 → 长表。
6. **词云表头偏移**：跳过前 2 行。
7. **"产品" 汇总表**：key-value → dict；**同时抽取首行 `导出时间`**。
8. **份额/百分比**：float 精度 round；品牌名 **trim + 小写归一**（供 AOSU 匹配）。
9. **列名规范化**：去首尾空格。

### 3.4 文件解析（配置驱动）

```python
DATA_DIR = Path(__file__).parent.parent / "data"
FILE_MAP = {
    "brand":          "BSR品牌销量.xlsx",
    "asin":           "BSR ASIN产品列表.xlsx",
    "keyword":        "BSR相关关键词.xlsx",
    "new_release":    "New Realese 产品列表.xlsx",
    "sales_trend":    "*_售出件数趋势.xlsx",
    "market_trend":   "*_市场趋势.xlsx",          # 排除 "(1)"
    "market_trend_2": "*_市场趋势 (1).xlsx",
    "global":         "*_全球售卖对比分析.xlsx",
    "wordcloud":      "*_Sorftime流量圈词云详情表-*.xlsx",
}
```

### 3.5 缓存策略

```python
@st.cache_data(show_spinner="加载数据...")
def load_all() -> dict:
    """读一次 → 清洗一次 → 返回所有清洗后 DataFrame 字典 + data_time。"""
```

- 返回结构新增 `data_time`：`{导出时间(ASIN/NewRelease)、销量趋势最新月份}`，供 Hello 页展示「数据时间来源」。

### 3.6 全局主题（`.streamlit/config.toml`）

```toml
[theme]
base = "light"
primaryColor = "#4E79A7"
backgroundColor = "#FAFAFA"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#2F3B52"

[server]
headless = true
```

> 原生图表配色由 Streamlit 主题驱动，与 ECharts 统一为同一主色。

---

## 4. KPI 指标口径

| KPI | 口径 | 数据源 |
|---|---|---|
| 市场月销量（当月/上月） | 最新两月的 `售出件数` | 售出件数趋势 |
| 市场月销售额（当月/上月） | 最新两月的 `净销售额($)` | 售出件数趋势 |
| 品牌数 | 57 | ASIN `产品`·品牌数量 |
| ASIN 数 | 100（Top）/ 27,542（类目全量） | ASIN `产品` |
| Seller 数 | 55 | ASIN `产品`·卖家数量 |
| 平均价格 / 平均星级 | $104.12 / 4.3 | ASIN `产品` |
| 新品占比 | 8%（BSR）/ 45.71%（NR） | 各自 `产品` 汇总 |
| 占比第一品牌 | eufy Security，销售额份额 29.45% | 品牌表（按销售额份额排序） |
| AOSU 排名 / 份额 | 第 4 位，销售额份额 8.53% | 品牌表（case-insensitive 匹配） |
| CR10（品牌集中度） | Top10 品牌销量份额之和 | 品牌表 |
| 环比（MoM） | `本月/上月 - 1`（按月） | 售出件数趋势 / 市场趋势 |
| 同比（YoY） | `本月/去年同月 - 1`（按年） | 售出件数趋势 / 市场趋势 |

> 计算封装在 `utils/kpi.py`：`sales_trend_kpis`、`mom_yoy_table`、`seasonality`、`brand_kpis`。

---

## 5. 页面设计

### 5.0 全局布局

- **每页顶部调用 `ui.setup_page(title)`**（内部 `st.set_page_config(page_title, page_icon, layout="wide")`）。Streamlit multipage 下各页面是独立脚本，`set_page_config` **不会在页间继承**，必须每页调用（原文档「只在 Hello.py 调用一次」的说法已纠正）。
- 侧边栏用 Streamlit multipage 自动导航（不引入 option-menu）。

### 5.1 Hello.py — 市场总览

- **数据时间来源**：标题下展示「数据来源：Sorftime 专业版导出 · 导出时间 2026-07-01 · 销量趋势截至 2026-07」。
- **KPI 卡（当月 + 上月 + 环比）**：市场月销量、市场月销售额（带 上月值 + 环比%）；品牌数、ASIN 数、Seller 数（快照值）。
- 图 1：销量/销售额月度趋势（**双轴**：售出件数 + 净销售额）。
- 图 2：**环比趋势（MoM%）** —— 按月，绿升红降柱状。
- 图 3：**同比趋势（YoY%）** —— 按年，绿升红降柱状。
- 图 4：全球市场销量（横向 bar）。
- 图 5：价格带分布（Histogram）。
- 图 6：Brand Market Share（Treemap）+ Top10 品牌（横向 bar）。
- 图 7：**Top20 产品**（Table：主图缩略 + 产品名称 + **ASIN** + **链接（URL）** + 销量/销额/星级/评价）。

### 5.2 1_BSR 品牌分析

- **KPI**：品牌数量、CR10（销量份额）、**占比第一品牌（eufy Security · 销售额份额 29.45%）**、**AOSU（排名 #4 · 销售额份额 8.53%）**。
- 图表：品牌销量/销售额排行（横向 bar）、销量 & 销额份额（环形图）、销量 vs 销额（气泡，气泡=销量份额）、销量帕累托图、**品牌价格定位（ASP 排行 + 价格定位气泡：x=ASIN数 · 气泡=月销量）**、原始数据表。

### 5.3 2_BSR ASIN 分析

- KPI：产品数、月总销量、平均价格、平均星级、平均评价。
- 图表：Top15 销量/销额（横向 bar）、价格 vs 销量、评分 vs 销量、评论 vs 销量、生命周期（上架天数 vs 销量，气泡=评价数）、毛利×销量四象限（气泡=毛利率）、**销量集中度（Top 1/3/5/10/20 累计份额）、价格区间/评分区间（环形 + 横向 bar）、店铺（卖家）对比（价格定位气泡 + 月销量排行）、ASIN 装修 & 卖家属性（物流/A+/主图视频/品牌旗舰店/自营占比 环形图）**、原始数据表。

### 5.4 3_BSR 关键词分析

- KPI：关键词数量、平均月搜索量、平均竞品数量、平均 CPC。
- 图表：Top20 搜索量排行（横向 bar）、搜索量分布（Histogram）、竞品数量 vs 搜索量（散点）、Top50 Treemap、流量圈词云 Treemap、**竞价与竞争度（搜索量 vs CPC 竞价，气泡=竞品数量；自然位 vs 广告位 竞争度散点，`kpi.competition_ratio` 解析 `255 / 81（3.15）`）**、**转化率与垄断性（搜索转化率 vs 点击转化率；点击垄断性 vs 转化垄断性，气泡=月搜索量）**、**增长趋势（关键词年度总搜索量 2024/2025/2026 折线；近6月 vs 近12月 复合增长率散点，裁尾异常值）**、标题词分析（从 Top100 标题挖词：高频词 Treemap + 高频词组 bigram 横向 bar）、原始数据表（扩充 `年搜索量-2024/2025年`、`cpc最低/最高竞价`、`近90日搜索/点击转化率`、`周搜索排名变化`、`复合增长率-近6/12个月`、`曝光转化垄断性` 等闲置字段）。

### 5.5 4_New Release 分析（新品机会）

- KPI：新品数量、新品月总销量、新品占比、平均价格、平均星级。
- 图表：新品销量 Top15（横向 bar，`品牌 · ASIN`）、上架时间分布（Histogram）、新品价格带、变体价格 vs 子体销量（散点）、品牌/卖家新品数量、**属性词挖掘（五点描述/属性列挖词：覆盖新品数 + 关联月销量 横向 bar）**、原始数据表。

### 5.6 5_市场趋势分析（重做）

> 参考 `Dome Surveillance Cameras.pdf` 的分析角度，从「简单堆叠折线」升级为「趋势 + 季节性 + 增速」三层结构。

- **头部**：类目元信息（类目名 / 一级大类 / NODEID）+ 数据截至月份 + 未完整月份提示。
- **概览 KPI**：最新月类目销量、同比、环比。
- **模块 A 规模趋势**：类目销量 & 类目销售额（双轴，全历史 95 个月）。
- **模块 B 同比/环比**：类目销量同比（YoY）柱状（绿升红降）+ 环比（MoM）。
- **模块 C 季节性**：类目销量 **热力图**（年份 × 12 月）—— 识别旺季/淡季。
- **模块 D 指标自由选择**：multiselect 多指标叠加折线（10 个指标）。
- **模块 E 价格与评价**：平均售价 / 平均星级 / 平均评价趋势。
- **模块 F 竞争格局**：平均品牌数量 / 平均卖家数量趋势。
- **原始数据表**。

### 5.7 6_全球市场分析

- 数据源：全球售卖对比分析。
- 图表：各站点销量/销额（横向 bar）、垄断系数对比（横向 bar）、新品占比对比（横向 bar）、站点概览表（含货币）。
- 澳洲/沙特「未找到相似类目」→ 灰色标注 / 单独说明。

---

## 6. 页面交互规范

- 统一 multipage 侧边导航；页面内分节标题。
- 全局筛选：时间范围（影响趋势类图表，v1 用最近 N 月下拉）。
- 图表交互：ECharts 原生 hover / 缩放 / 图例点击；`st.dataframe` 排序/筛选/下载。
- 下载：`st.dataframe` 自带 CSV 下载；ECharts 可用工具栏导出图片。
- 表格与图表联动：v1 用下拉筛选联动。

---

## 7. 图表设计规范

- **统一高度 420px**（Treemap/热力图 480px）。
- **响应式宽度**：ECharts `width="100%"`；原生图表 `width="stretch"`。
- **配色（`utils/theme.py` 常量，统一简约）**：
  - 主色 `#4E79A7`；辅助色 `#59A14F #F28E2B #E15759 #76B7B2 #A0CBE8`。
  - 背景 `#FAFAFA`、卡片白底、文字主色 `#2F3B52`、弱化文字 `#8A94A6`。
  - 涨跌色：**涨 `#59A14F` / 跌 `#E15759`**（环比/同比柱）。
- **图表类型分配**：
  - Streamlit 原生：折线（单/多指标）、面积、纵向柱、直方图、简单散点。
  - ECharts：横向条、环形、Treemap、帕累托、双轴折线、热力图、气泡（size/color）、涨跌柱。
- **数字格式**（`utils/formatters.py`）：千分位、百分比、货币、日期、环比/同比（`+x.x%`），全局统一，禁止页面裸写 `f"{x:,}"`。
- **坐标轴/网格**：淡色虚线网格 `#EEF2F7`、去多余刻度线；ECharts tooltip 用 `JsCode` 做千分位格式化。

---

## 8. 开发规范（强化）

1. 数据处理封装在 `data_loader.py`；图表封装在 `charts.py`；KPI/环比/同比/季节性封装在 `kpi.py`；页面只负责布局。
2. 不允许重复代码（`set_page_config`、使用说明、KPI 卡片统一到 `components/ui.py`）。
3. 所有图表高度统一、数字格式统一、响应式、色彩统一。
4. 耗时计算用 `@st.cache_data`。
5. 每页加载目标 ≤ 2 秒。
6. 列名引用用常量（`data_loader.py` 顶部），页面不写魔法字符串。

---

## 9. 后续扩展

- 多站点 / 多类目切换（数据层已预留站点字段、NODEID/类目名）。
- 同比环比 / 时间维度分析（95 个月历史已支持）。
- 数据自动更新（缓存键含文件 mtime）。
- AI 自动生成市场分析总结（LLM）。

---

## 附 A：本次修改点 → 实现对照表

| # | 修改点 | 实现 |
|---|---|---|
| 1 | Plotly → Streamlit 原生 + ECharts | 重写 `utils/charts.py`；移除 plotly 依赖 |
| 2 | 色彩统一简约 | 精简 `theme.py` 色板 + `.streamlit/config.toml` 统一主题 |
| 3 | Hello 加数据时间来源 | `data_loader` 抽取导出时间 + 销量最新月份，Hello 标题下展示 |
| 4 | KPI 卡加当月/上月 | `sales_trend_kpis` + `ui.kpi_cards` 支持 prev/delta |
| 5 | 环比/同比图（按月/按年） | `mom_yoy_table` + 涨跌柱状图 |
| 6 | Top20 加 ASIN + 链接 | `st.column_config.LinkColumn`（URL）+ ASIN 列 |
| 7 | 品牌页占比第一品牌 + AOSU | `brand_kpis` 返回 Top 品牌 / AOSU 排名与份额 |
| 8 | 市场历史趋势页重做 | §5.6 三层结构（趋势/季节性/增速） |
| 9 | 参考 PDF 分析角度 | 季节性热力图、品牌集中度、ASIN 气泡 |
| 10 | 参考 PDF 逐页补充「可直接实现」图表 | 品牌/店铺价格定位、Listing 集中度、价格/评分区间、店铺对比、ASIN 装修占比、标题词云/词组、新品属性词挖掘（`utils/kpi.py` 新增聚合函数，见 §5.2~5.5） |
| 11 | 关键词页闲置字段入图 | `BSR相关关键词.xlsx` 的 `cpc最低/最高竞价`、`年搜索量-2024/2025年`、`近90日搜索/点击转化率`、`自然位/广告位竞争`（`kpi.competition_ratio` 解析竞争度）、`复合增长率-近6/12个月`、`曝光转化垄断性` 补入图表（见 §5.4） |

## 附 B：代码核对发现的失误点 → 修正清单

| # | 失误点 | 修正 |
|---|---|---|
| 1 | `set_page_config` 只在 Hello.py，其余页非 wide 布局 | 每页顶部 `ui.setup_page()` |
| 2 | 市场趋势「11 指标」误写 | 实为 10 指标 |
| 3 | 市场趋势转置尾部留空月份行 | 剔除尾部空月份 |
| 4 | 未抽取「导出时间」数据来源 | `data_loader` 抽取并随 `data_time` 返回 |
| 5 | 未处理最新月未完整（环比异常） | 环比/同比图与 KPI 标注「数据可能不完整」 |
| 6 | AOSU 品牌名大小写不一致 | 品牌名归一后 case-insensitive 匹配 |
| 7 | 关键词「坑位竞争需拆 3 列」误判 | 实际已是 3 列，无需拆分 |
| 8 | requirements 含未用依赖（plotly 等） | 精简为 streamlit/pandas/numpy/openpyxl/streamlit-echarts |
| 9 | Top20 产品缺 ASIN/链接 | 增加 ASIN + LinkColumn |
| 10 | 无全局主题，原生图表配色不统一 | 新增 `.streamlit/config.toml` |
